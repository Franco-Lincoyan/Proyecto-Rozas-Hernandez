import 'package:flutter/material.dart';

class Entre_Page extends StatelessWidget {
  const Entre_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ENTRASTE'),
    );
  }
}